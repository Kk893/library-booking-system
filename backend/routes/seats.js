const express = require('express');
const router = express.Router();

// Placeholder for Seats routes
router.get('/', (req, res) => {
  res.json({ message: 'Seats routes not implemented yet' });
});

module.exports = router;