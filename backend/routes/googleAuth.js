const express = require('express');
const router = express.Router();

// Placeholder for Google Auth routes
router.get('/google', (req, res) => {
  res.json({ message: 'Google Auth not implemented yet' });
});

module.exports = router;